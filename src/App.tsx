import React from "react";

export default function App() {
  return (
    <main
      style={{
        minHeight: "100vh",
        background: "#050B18",
        color: "rgba(255,255,255,0.92)",
        padding: 18,
        fontFamily: "system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif",
      }}
    >
      <h1 style={{ marginTop: 0 }}>BALANCE Cipher V2 — Baseline</h1>
      <p>If you can see this, React is rendering and Vercel is building correctly.</p>
    </main>
  );
}

